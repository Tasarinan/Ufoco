export const app = (state) => state.app;
export const auth = (state) => state.auth;
export const file = (state) => state.file;
export const element = (state) => state.bufl;
export const elements = (state) => state.file.elements;
